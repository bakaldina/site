import Vue from 'vue'
import Vuetify from 'vuetify/lib'
import colors from 'vuetify/es5/util/colors'

Vue.use(Vuetify)

const vuetify = new Vuetify({
    theme: {
        // primary: '#7000ff',
        // secondary: '#ff7200',
        // accent: '#8c9eff',
        // error: '#b71c1c',
        // rrr: '#ff7200'
    },

})
